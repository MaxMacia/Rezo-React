import { HeaderContainer, HeaderTitle } from "./Header.style";

function Header() {
    return (
        <HeaderContainer>
            <HeaderTitle>Rezo</HeaderTitle>
        </HeaderContainer>
    );
};

export default Header;